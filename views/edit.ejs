<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <h1 class="nav-title">
                <i class="fas fa-users"></i>
                Person Management System
            </h1>
            <div class="nav-links">
                <a href="/person" class="nav-link">
                    <i class="fas fa-list"></i> View All
                </a>
                <a href="/person/new" class="nav-link">
                    <i class="fas fa-plus"></i> Add Person
                </a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h2><i class="fas fa-user-edit"></i> Edit Person</h2>
                <a href="/person" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Back to List
                </a>
            </div>

            <div class="form-container">
                <form action="/person/<%= person._id %>?_method=PUT" method="POST" class="person-form">
                    <div class="form-group">
                        <label for="name">
                            <i class="fas fa-user"></i> Name *
                        </label>
                        <input 
                            type="text" 
                            id="name" 
                            name="name" 
                            value="<%= person.name || '' %>" 
                            required
                            maxlength="50"
                            class="<%= errors && errors.name ? 'error' : '' %>"
                        >
                        <% if (errors && errors.name) { %>
                            <span class="error-message">
                                <i class="fas fa-exclamation-circle"></i>
                                <%= errors.name %>
                            </span>
                        <% } %>
                    </div>

                    <div class="form-group">
                        <label for="age">
                            <i class="fas fa-calendar-alt"></i> Age *
                        </label>
                        <input 
                            type="number" 
                            id="age" 
                            name="age" 
                            value="<%= person.age || '' %>" 
                            required
                            min="0"
                            max="150"
                            class="<%= errors && errors.age ? 'error' : '' %>"
                        >
                        <% if (errors && errors.age) { %>
                            <span class="error-message">
                                <i class="fas fa-exclamation-circle"></i>
                                <%= errors.age %>
                            </span>
                        <% } %>
                    </div>

                    <div class="form-group">
                        <label for="gender">
                            <i class="fas fa-venus-mars"></i> Gender *
                        </label>
                        <select 
                            id="gender" 
                            name="gender" 
                            required
                            class="<%= errors && errors.gender ? 'error' : '' %>"
                        >
                            <option value="">Select Gender</option>
                            <option value="Male" <%= person.gender === 'Male' ? 'selected' : '' %>>Male</option>
                            <option value="Female" <%= person.gender === 'Female' ? 'selected' : '' %>>Female</option>
                            <option value="Other" <%= person.gender === 'Other' ? 'selected' : '' %>>Other</option>
                        </select>
                        <% if (errors && errors.gender) { %>
                            <span class="error-message">
                                <i class="fas fa-exclamation-circle"></i>
                                <%= errors.gender %>
                            </span>
                        <% } %>
                    </div>

                    <div class="form-group">
                        <label for="mobileNumber">
                            <i class="fas fa-phone"></i> Mobile Number *
                        </label>
                        <input 
                            type="tel" 
                            id="mobileNumber" 
                            name="mobileNumber" 
                            value="<%= person.mobileNumber || '' %>" 
                            required
                            pattern="[0-9]{10}"
                            placeholder="1234567890"
                            maxlength="10"
                            class="<%= errors && errors.mobileNumber ? 'error' : '' %>"
                        >
                        <small class="form-hint">Enter 10-digit mobile number without spaces or symbols</small>
                        <% if (errors && errors.mobileNumber) { %>
                            <span class="error-message">
                                <i class="fas fa-exclamation-circle"></i>
                                <%= errors.mobileNumber %>
                            </span>
                        <% } %>
                    </div>

                    <% if (errors && errors.general) { %>
                        <div class="alert alert-error">
                            <i class="fas fa-exclamation-circle"></i>
                            <%= errors.general %>
                        </div>
                    <% } %>

                    <div class="form-actions">
                        <button type="submit" class="btn btn-primary">
                            <i class="fas fa-save"></i> Update Person
                        </button>
                        <a href="/person" class="btn btn-secondary">
                            <i class="fas fa-times"></i> Cancel
                        </a>
                    </div>
                </form>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 Person Management System. Built with Node.js & MongoDB.</p>
        </div>
    </footer>
</body>
</html>