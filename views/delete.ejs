<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>
    <link rel="stylesheet" href="/styles.css">
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.0.0/css/all.min.css" rel="stylesheet">
</head>
<body>
    <nav class="navbar">
        <div class="nav-container">
            <h1 class="nav-title">
                <i class="fas fa-users"></i>
                Person Management System
            </h1>
            <div class="nav-links">
                <a href="/person" class="nav-link">
                    <i class="fas fa-list"></i> View All
                </a>
                <a href="/person/new" class="nav-link">
                    <i class="fas fa-plus"></i> Add Person
                </a>
            </div>
        </div>
    </nav>

    <main class="main-content">
        <div class="container">
            <div class="page-header">
                <h2><i class="fas fa-user-times"></i> Delete Person</h2>
                <a href="/person" class="btn btn-secondary">
                    <i class="fas fa-arrow-left"></i> Back to List
                </a>
            </div>

            <div class="delete-container">
                <div class="warning-card">
                    <div class="warning-icon">
                        <i class="fas fa-exclamation-triangle"></i>
                    </div>
                    <h3>Are you sure you want to delete this person?</h3>
                    <p class="warning-text">
                        This action cannot be undone. The person's information will be permanently removed from the system.
                    </p>
                </div>

                <div class="person-details-card">
                    <h4><i class="fas fa-user"></i> Person Details</h4>
                    <div class="details-grid">
                        <div class="detail-item">
                            <strong>Name:</strong>
                            <span><%= person.name %></span>
                        </div>
                        <div class="detail-item">
                            <strong>Age:</strong>
                            <span><%= person.age %> years</span>
                        </div>
                        <div class="detail-item">
                            <strong>Gender:</strong>
                            <span><%= person.gender %></span>
                        </div>
                        <div class="detail-item">
                            <strong>Mobile Number:</strong>
                            <span><%= person.mobileNumber %></span>
                        </div>
                        <div class="detail-item">
                            <strong>Created:</strong>
                            <span><%= new Date(person.createdAt).toLocaleDateString() %></span>
                        </div>
                        <div class="detail-item">
                            <strong>Last Updated:</strong>
                            <span><%= new Date(person.updatedAt).toLocaleDateString() %></span>
                        </div>
                    </div>
                </div>

                <div class="delete-actions">
                    <form action="/person/<%= person._id %>?_method=DELETE" method="POST" style="display: inline;">
                        <button type="submit" class="btn btn-danger" onclick="return confirm('Are you absolutely sure you want to delete this person?')">
                            <i class="fas fa-trash"></i> Yes, Delete Person
                        </button>
                    </form>
                    <a href="/person" class="btn btn-secondary">
                        <i class="fas fa-times"></i> Cancel
                    </a>
                </div>
            </div>
        </div>
    </main>

    <footer class="footer">
        <div class="container">
            <p>&copy; 2025 Person Management System. Built with Node.js & MongoDB.</p>
        </div>
    </footer>
</body>
</html>